# dependencies
node_modules/
.pnp/
.pnp.js

# next.js
.next/
out/
build/

# turborepo local cache
.turbo/

# firebase (deploy cache; keep firebase.json / .firebaserc tracked)
.firebase/

# static-export build stash (scripts/build-static.sh). If this exists, a build
# died mid-flight and app/api is currently MOVED OUT of the tree — restore it,
# never commit the deletion.
.api-stash/

# claude code local settings
.claude/

# vercel
.vercel/

# typescript
*.tsbuildinfo
next-env.d.ts

# env — cover EVERY .env variant (.env.production, .env.development, .env.test
# were previously unignored and would have been committed to this PUBLIC repo).
.env*
!.env.example

# debug
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# os
.DS_Store
Thumbs.db

# editor
.idea/
.vscode/
*.swp
.vercel

# Reference files
*.txt
!README.txt

FILES/
files.zip

# Reference product ZIPs (kept locally, not tracked)
PRODUCTS/*.zip
